#include <stdio.h>
#include <string.h>

int main(){
    // char s[1001], t[1001]; 

    // scanf("%s %s", s, t); //input = Level Up
    
    // int sLength = strlen(s);
    // int tLength = strlen(t);
    // printf("%d %d\n", sLength, tLength);

    // printf("%s %s", s, t);

  return 0;
}